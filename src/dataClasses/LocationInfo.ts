
/**
 * Stores basic info about root location. Used to display info in order the user
 * could choose {@link Location}, to explore.
 */
export default interface RootLocationInfo {
    id: string
    name: string
}